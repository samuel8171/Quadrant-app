# liquid-glass-react@1.1.1 声明的 peer 是 react >= 19，而本项目跑在 React 18.3.1 上，
# 并且已经在「双引擎玻璃材质 + 几何 + 动画」上大量实测（见 docs/probes/liquid-glass.md），
# 不打算为了它升到 React 19。
#
# 本地当初就是用 --legacy-peer-deps 装的（node_modules 已存在，所以看不出来），
# 但 CI 是干净环境，`npm ci` 会在 ERESOLVE 上直接失败 —— 2026-09-25 实测：
#   npm error code ERESOLVE
#   While resolving: liquid-glass-react@1.1.1
#   Found: react@18.3.1 / peer react@">=19" from liquid-glass-react@1.1.1
#   Conflicting peer dependency: react@19.3.0
# 这条把它固化进仓库，让 CI 与本地一致。（两个 workflow 都会跑 `npm ci`。）
legacy-peer-deps=true
