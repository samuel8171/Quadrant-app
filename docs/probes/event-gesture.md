# 事件块手势实测（390×844 触摸视口）

生成时间：2026/9/23 14:12:06

当前生效的 .day-event touch-action = `none`

| 场景 | 滚动Δ | 块顶边前 | 块顶边后 | 块随滚动 | 块被拖动 | 见过 armed | 见过 dragging |
| --- | --- | --- | --- | --- | --- | --- | --- |
| A. 块上纵滑（想滚动） | +110 | 365 | 255 | 是 | 否 | 否 | 否 |
| B. 长按解锁后拖动 | +0 | 365 | 317 | 否 | **是** | 是 | 是 |
| C. 短促轻触 | +0 | 317 | 317 | 是 | 否 | 否 | 否 |

## 判读

- A（块上纵滑）：滚动 Δ = **110px**，块被拖动 = **否** → ✅ 滚动归浏览器，块未被误移动
- B（长按解锁后拖动）：块被拖动 = **是**，armed 高亮 = **见过** → ✅ 长按解锁路径有效
- C（短促轻触）：块被拖动 = **否** → ✅ 轻触不移动块


> 注：Chromium 的触摸模拟与 WebKit 并非等价，本探针证明的是「状态机与 CSS 取值生效」，
> iOS 真机验收仍不可替代。