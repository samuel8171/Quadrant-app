# 底部导航 58px → 66px 的几何代价实测

生成时间：2026/9/23 14:02:58

方法：同一页面内先后测量「当前构建」与「注回改动前尺寸」，避免回滚代码。

| 指标 | 375×667 | 390×844 | 430×932 |
| --- | --- | --- | --- |
| 导航条高 | 66px（+8） | 66px（+8） | 66px（+8） |
| 导航条内边距(纵) | 5px（-1） | 5px（-1） | 5px（-1） |
| 导航条圆角 | 24px（+2） | 24px（+2） | 24px（+2） |
| 单个入口高 | 54px（+10） | 54px（+10） | 54px（+10） |
| 入口圆角 | 21px（+1） | 21px（+1） | 21px（+1） |
| 右上按钮高 | 0px | 0px | 0px |
| 右上按钮顶边 | 0px | 0px | 0px |
| 页面 padding-top | 12px | 12px | 12px |
| 页面 padding-bottom | 82px（+8） | 82px（+8） | 82px（+8） |
| 导航顶边(=内容净高) | 591px（-8） | 768px（-8） | 856px（-8） |

## 结论

- 导航条本体增高 **8px**（58 → 66），单个入口触控高从 44px 增到 **54px**（+10）。
- 代价：内容区净高从 776px 减到 **768px**，即 **-8px**（视口高的 0.9%）。
- 入口触控高 54px 已明显超过 iOS HIG 的 44px 下限，达到 1.2×；此前 44px 是刚好过线。
